/**
 * the Server class should expose the described methods, (get, post, listen) and should have
 *  a constructor accepting a @{integer} port as a parameter see the use cases in the index file, for
 * more informations
 */
class Server {}

// don't forget to export your class definition so it
// can be invocable outside of the module
module.exports = Server;
